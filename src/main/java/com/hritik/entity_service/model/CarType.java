package com.hritik.entity_service.model;

public enum CarType {
    MINI,           // Small hatchbacks, low fare (e.g., WagonR, Indica)
    SEDAN,          // 4-seater sedans (e.g., Dzire, Xcent)
    SUV,            // 6–7 seater SUVs (e.g., Ertiga, Innova)
    LUXURY,         // Premium cars (e.g., BMW, Mercedes)
    AUTO,           // Auto rickshaw
    BIKE,           // Bike taxi
    RENTAL,         // Hourly rental cars
    OUTSTATION      // Intercity rides
}
